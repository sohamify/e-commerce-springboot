spring:
  datasource:
    url: jdbc:postgresql://${SUPABASE_POOLER_HOST}:6543/postgres?sslmode=require&prepareThreshold=0
    username: ${SUPABASE_POOLER_USER}
    password: ${SUPABASE_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  data:
    redis:
      host: ${UPSTASH_REDIS_HOST}
      port: ${UPSTASH_REDIS_PORT:6379}
      password: ${UPSTASH_REDIS_PASSWORD}
      ssl:
        enabled: true
  cloud:
    gcp:
      project-id: e-commerce-bb89d
  flyway:
    enabled: true
    url: jdbc:postgresql://${SUPABASE_DB_HOST}:5432/postgres?sslmode=require
    user: ${SUPABASE_DB_USER}
    password: ${SUPABASE_DB_PASSWORD}
app:
  storage:
    bucket: ${STORAGE_BUCKET:e-commerce-bb89d.firebasestorage.app}